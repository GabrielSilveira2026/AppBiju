import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    containerContent: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        padding: 8
    },
    backgroundImage: {
        flex: 1,
        resizeMode: 'cover',
        justifyContent: 'center',
    }
});